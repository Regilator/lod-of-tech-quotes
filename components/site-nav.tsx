'use client'
import Link from 'next/link'
import { useState } from 'react'
import { Menu, X, Bell, ChevronDown } from 'lucide-react'
const categories = ['Love','Romantic','Psychological','Sonnets','Religious','Cultural','Lyrical','Sad','Emotional','Comforting','Motivational']
export function SiteNav() { const [open, setOpen] = useState(false); return <header className="relative z-20 border-b border-ink/10 bg-paper/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 md:px-8"><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center border border-gold font-serif text-xl text-gold">L</span><span className="hidden text-xs font-bold uppercase tracking-[.25em] text-ink sm:block">Lod of Tech <span className="block font-normal tracking-[.12em] text-mutedgold">Quotes</span></span></Link><nav className="hidden items-center gap-7 text-xs uppercase tracking-[.16em] text-ink/70 lg:flex"><Link href="/" className="hover:text-gold">Home</Link><div className="group relative"><button className="flex items-center gap-1 hover:text-gold">Categories <ChevronDown className="size-3" /></button><div className="invisible absolute left-0 top-6 grid w-72 grid-cols-2 gap-1 border border-ink/10 bg-paper p-3 opacity-0 shadow-lg transition group-hover:visible group-hover:opacity-100">{categories.map(c => <Link key={c} href={`/category/${c.toLowerCase()}`} className="px-2 py-2 hover:bg-champagne/40">{c}</Link>)}</div></div><Link href="#daily" className="hover:text-gold">Quote of the Day</Link><Link href="#about" className="hover:text-gold">About</Link><Link href="#requests" className="hover:text-gold">Requests</Link></nav><div className="flex items-center gap-4"><button aria-label="Notifications" className="text-ink/60 hover:text-gold"><Bell className="size-4" /></button><Link href="/profile" className="hidden border border-ink px-4 py-2 text-xs uppercase tracking-[.16em] transition hover:border-gold hover:text-gold sm:block">Sign in</Link><button onClick={() => setOpen(!open)} className="lg:hidden" aria-label="Toggle menu">{open ? <X /> : <Menu />}</button></div></div>{open && <div className="flex flex-col gap-4 border-t border-ink/10 px-5 py-5 text-sm uppercase tracking-[.14em] lg:hidden">{['Home','Quote of the Day','About','Requests','Profile'].map(item => <Link key={item} href={item === 'Home' ? '/' : `#${item.toLowerCase().replaceAll(' ','-')}`} onClick={() => setOpen(false)}>{item}</Link>)}</div>}</header> }

